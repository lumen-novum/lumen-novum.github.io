# Windows 98 Digital Portfolio

A Jekyll boilerplate styled like Windows 98 and ready for GitHub Pages.

## Local preview
1. Install Ruby and Bundler.
2. Run `bundle install`.
3. Run `bundle exec jekyll serve`.
4. Open `http://localhost:4000`.
